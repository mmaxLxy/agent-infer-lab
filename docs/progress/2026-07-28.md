# 2026-07-28 阶段工作记录

## 今日目标

把已有的抽象工作负载 `RequestSpec` 转换为可以直接发送给 vLLM 的精确 Token 请求，为后续流式 HTTP 压测建立可复现的输入。

## 完成内容

### 1. 固化流式并发压测方案

- 当前功能分支为 `agent/streaming-benchmark`。
- 已完成固定并发流式压测设计文档和详细实施计划。
- 明确使用 vLLM `/tokenize` 校准输入，使用 `/v1/completions` 发送 Token ID。
- 明确采用 Python 标准库、固定并发闭环调度和 CPU 可运行的单元测试。

### 2. 精确 Token Prompt 构造

- 新增不可修改的 `PreparedRequest`，保存请求编号、精确输入 Token ID 和最大输出 Token 数。
- 新增 `prepare_requests()`，把 `RequestSpec` 转换为真实可发送请求。
- 使用确定性 Agent 类种子文本建立 Token 池。
- 输入长度不足时循环读取 Token 池，保证最终 Token 数严格等于 `input_tokens`。
- 根据 `shared_prefix_tokens` 构造相同前缀，为后续 Prefix Cache 实验准备工作负载。
- 使用请求序号产生确定性偏移，使不同请求具有不同后缀。
- 构造过程只调用一次 Tokenize 函数，不改变全局随机状态。

### 3. 输入校验与自动测试

- 拒绝空请求集合和空种子文本。
- 拒绝空 Token 池、布尔值、负数 Token ID。
- 拒绝超过输入长度的共享前缀。
- 验证精确输入长度、输出设置、共享前缀、独立后缀和确定性。
- 验证 `PreparedRequest` 创建后不可修改。
- Prompt 模块新增 12 项测试。

## 验证证据

```text
pytest tests/test_prompting.py -q
12 passed in 0.14s

pytest -q
56 passed in 0.24s

ruff check .
All checks passed!

/home/ayax/.local/bin/uv lock --check
Using CPython 3.12.3 interpreter at: /usr/bin/python3
Resolved 8 packages in 2ms

git diff --check
无错误输出
```

## Git 记录

- 设计提交：`311edb2 docs: design streaming inference benchmark`
- 计划提交：`0816c71 docs: plan streaming inference benchmark`
- 功能提交：`2103527 feat: build exact-token prompts`
- 功能变更：2 个文件，新增 203 行。
- 当前工作分支：`agent/streaming-benchmark`
- 今日不推送、不创建 PR、不合并到 `main`。

## 本阶段的作用

此前的 `RequestSpec` 只描述“需要多少 Token”，并不包含能发给模型的真实内容。本阶段把抽象规格转换为精确 Token ID，使后续不同并发、不同共享前缀和 CUDA 优化实验使用相同输入，避免字符长度或重新分词造成实验条件漂移。

## 当前边界

- 尚未实现 vLLM HTTP 客户端。
- 尚未解析 `/v1/completions` 流式响应。
- 尚未执行固定并发调度。
- 尚未采集真实请求的 TTFT、TPOT 和 E2E。
- 尚未进入 CUDA 算子开发。

## 下一步

1. 实现标准库 vLLM 客户端。
2. 调用 `/tokenize` 获取真实 Token ID。
3. 调用 `/v1/completions` 并解析 SSE 流。
4. 记录请求开始、首 Token 和完成时间，生成 `RequestTrace`。
