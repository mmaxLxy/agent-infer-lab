# 2026-07-22 阶段工作记录

## 完成项

### 1. 确定性请求生成器

- 新增不可修改的 `RequestSpec`，记录请求编号、输入 Token 数、输出 Token 数和共享前缀 Token 数。
- 新增 `generate_workload()`，根据 `WorkloadConfig` 批量生成请求规格。
- 使用函数内部独立的随机数生成器；相同配置和随机种子会得到相同结果，同时不会污染程序其他部分的随机状态。
- 为每个请求生成稳定且唯一的编号，例如 `req-000000`。

### 2. 完整质量检查

- 新增测试覆盖不可修改性、重复生成一致性、长度候选约束、唯一编号、共享前缀计算和全局随机状态隔离。
- 运行全部项目测试、全项目 Ruff 检查和依赖锁检查。

## 验证证据

```text
pytest tests/test_workloads.py -q
18 passed in 0.11s

pytest -q
23 passed in 0.10s

ruff check .
All checks passed!

/home/ayax/.local/bin/uv lock --check
Using CPython 3.12.3 interpreter at: /usr/bin/python3
Resolved 8 packages in 3ms
```

- 本地提交：`7f4949f feat: generate deterministic workloads`

## 问题与决策

- 问题：生成器测试最初因 `RequestSpec` 和 `generate_workload` 尚不存在而导入失败。
- 判断：这是测试先行阶段的预期失败；补充实现后，同一组测试全部通过。
- 决策：本阶段只生成请求规格，不生成真实文本、不发送 HTTP 请求，也不执行 GPU 推理，保证 CPU CI 不依赖 CUDA、vLLM 或 Hugging Face。

## 下一步

1. 审核并提交这两份阶段日志。
2. 再将请求规格接入后续的请求构造与 vLLM 压测客户端。
3. `concurrency` 当前只完成配置校验；实际并发调度将在客户端阶段实现。
