# 2026-08-09 阶段工作记录

## 今日目标

完成固定并发 vLLM 推理基准闭环：补齐调度测试和命令入口，在 RTX 4060 8 GiB 上执行真实流式请求，记录可复验的延迟与吞吐量。

## 完成内容

### 1. 固定并发 Runner 与命令行

- 新增 `run_benchmark()`，使用固定大小 `ThreadPoolExecutor` 执行闭环并发请求。
- 新增 `execute_benchmark()`，连接工作负载、Prompt、vLLM客户端和指标模块。
- 新增 `agent-infer-bench` 命令行入口。
- 支持服务地址、模型、请求数、并发数、输入/输出长度、共享前缀比例、随机种子和超时参数。
- 终端输出 TTFT、TPOT、E2E、P50/P99 和输出吞吐量。

### 2. CPU 自动测试

- 验证所有请求仅执行一次并进入指标汇总。
- 验证运行中请求数不超过配置的固定并发数。
- 验证请求异常向上层传播。
- 拒绝空请求、非正整数并发和超过请求数的并发。
- 验证已有组件连接和命令行输出。

验证结果：

```text
pytest tests/test_benchmark.py -q
10 passed in 0.18s

pytest -q
74 passed in 0.30s

ruff check .
All checks passed!

/home/ayax/.local/bin/uv lock --check
Resolved 8 packages in 2ms

git diff --check
无错误输出
```

### 3. 正式命令安装

- 在 `D:\agent-infer-lab\.venv` 创建项目本地运行环境。
- 项目没有新增第三方运行时依赖。
- `.venv/bin/agent-infer-bench --help` 验证成功。
- 使用正式命令完成4请求、并发2的真实GPU实验。

### 4. 真实 vLLM/GPU 验收

- GPU：NVIDIA GeForce RTX 4060，8188 MiB。
- vLLM：0.23.0+cu129。
- 模型：`Qwen/Qwen2.5-0.5B-Instruct`。
- 服务参数：`gpu-memory-utilization=0.75`，`max-model-len=2048`。
- `/v1/models` 成功返回模型信息。
- 实际使用 `/tokenize` 和 `/v1/completions` 流式接口。

冒烟实验：

```text
requests: 4
concurrency: 1
input_tokens: 64
output_tokens: 16
output_throughput_tokens_per_second: 53.985338
ttft_p50_seconds: 0.012763
ttft_p99_seconds: 0.813273
tpot_p50_seconds: 0.005307
tpot_p99_seconds: 0.005972
e2e_p50_seconds: 0.100544
e2e_p99_seconds: 0.890077
```

首次请求包含编译/预热影响，因此冒烟实验尾延迟较高。

稳定态固定并发实验：

```text
requests: 20
concurrency: 4
input_tokens: 128
output_tokens: 32
shared_prefix_ratio: 0.5
seed: 20260809
output_tokens_total: 640
duration_seconds: 1.036271
output_throughput_tokens_per_second: 617.599136
ttft_p50_seconds: 0.019865
ttft_p99_seconds: 0.100700
tpot_p50_seconds: 0.005431
tpot_p99_seconds: 0.006103
e2e_p50_seconds: 0.191964
e2e_p99_seconds: 0.267913
```

实验前显存占用7442 MiB，实验后7441 MiB；服务日志未出现 CUDA OOM、`OutOfMemoryError` 或 Python Traceback。

## 问题与决策

- vLLM首次启动需要约两分钟完成 `torch.compile` 和 CUDA Graph捕获，40秒健康检查不足；继续等待后服务正常就绪。
- 初始后台启动命令把整段命令链放入后台，日志文件没有生成；改为 `setsid` 独立会话后启动成功。
- 开发环境没有 pip，因此没有向 `/home` 环境追加安装；使用 uv 在项目目录的 `.venv` 安装命令入口，符合新内容放在D盘的约束。
- 不把首次预热实验和稳定态实验混为一组结果。

## 当前边界与下一阶段

- 第一版固定并发流式基准闭环已完成。
- 当前没有泊松到达、重试、Goodput、GPU利用率采样和结果文件持久化。
- 下一阶段基于本工具执行并发/长度/共享前缀矩阵实验，定位瓶颈后选择 CUDA Kernel 优化点。
