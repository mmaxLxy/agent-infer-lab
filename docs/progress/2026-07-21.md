# 2026-07-21 阶段工作记录

## 完成项

### 1. 工作负载配置模型

- 新增不可修改的 `WorkloadConfig`，统一保存请求数量、输入与输出 Token 长度候选、共享前缀比例、并发数和随机种子。
- 增加配置校验，拒绝空长度集合、非正整数、越界比例、并发数超过请求数等无效输入。
- 采用测试先行方式：先确认测试因模块不存在而失败，再补充最小实现使测试通过。

## 验证证据

```text
pytest tests/test_workloads.py -q
13 passed

ruff check src/agent_infer_lab/workloads.py tests/test_workloads.py
All checks passed!
```

- 本地提交：`2fa03e3 feat: validate workload configuration`

## 问题与决策

- 问题：最初测试因 `agent_infer_lab.workloads` 不存在而无法导入。
- 判断：这是测试先行阶段的预期失败，证明测试能够发现缺失功能。
- 决策：先只实现配置和校验，不提前连接 vLLM、CUDA 或生成真实文本，保持 CPU 测试快速且稳定。

## 下一步

1. 定义单个请求的数据结构。
2. 使用固定随机种子生成可重复的请求规格。
3. 验证生成过程不会修改 Python 的全局随机状态。
